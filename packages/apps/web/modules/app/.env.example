# URL base del API de NECTO.
# En desarrollo NO es necesario definirla: por defecto usa "/api", que vite
# redirige (proxy) a http://localhost:8080 (el microservicio local).
# Para apuntar a un backend desplegado, define aqui la URL completa:
# VITE_API_URL=https://<api-gateway-url>
