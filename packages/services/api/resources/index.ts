/**
 * Shared Resource Definitions — barrel export.
 *
 * Re-exports all shared resource classes (SDK subclasses, DataExport, ResourceExport)
 * that this bundle registers and exposes to consumer stacks.
 *
 * Imported within the bundle via @res alias.
 * Imported by consumers via @res:<artifact-name> alias.
 */
export { ResourceName } from './resource-name.js';
